import { Text, View } from "react-native";
import { styles } from "./styles";

const ShortlistScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={{ color: "white" }}>Shortlist Screen</Text>
    </View>
  );
};
export default ShortlistScreen;
